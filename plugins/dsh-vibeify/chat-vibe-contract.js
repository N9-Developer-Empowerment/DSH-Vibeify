export function buildChatVibeInstructions() {
  return `DSH Vibeify includes one shared edited Vibe surface alongside ordinary Chat. Chat is the detailed source view. After an ordinary DSH turn has durably ended as completed, its final assistant answer can be projected from local session history into a newest-first, browser-local Vibe card. An explicit public-content request can instead publish each complete verified \`chat-\` envelope while that same turn continues; final history remains the lossless fallback. This applies across DSH threads without reopening, prompting, or resuming them. Neither route starts an additional AI/model call. They must never copy the user's raw prompt, attachment, account data, session data, hidden reasoning, tool activity, approval details, aborted work, interrupted work, or incomplete progress.

When the user explicitly asks to **make, create, write, or turn something into a Vibe or Vibe article**, always treat that as a Vibe presentation request. This is the clearest unambiguous instruction for the richer visual, linked, progressively published route. A request to show, find, discover, browse, or recommend reader-facing public content may also be treated as a Vibe presentation request when the result benefits from a website-like set of linked cards. Research and verify it normally. For a multi-item request, publish the first bounded complete item before researching the whole series. Prefer a useful 80–180 word visual opening once its minimum claims, content link, and image provenance are verified; do not wait for every research lane or the long feature. Then publish each later item immediately when it is ready, while slower research and independent workers continue. Use one envelope per commentary update so a completed item is never held behind another item. As each semantic item becomes safe to present, publish it in commentary as exactly one complete closed envelope:

<vibe-chunk id="chat-unique-item-id" kind="recommendation" title="A reader-facing title">
Complete Markdown for this item, with relevant verified links and creator/source credit.
</vibe-chunk>

Allowed kinds are article, editorial, recommendation, image, music, video, and questionnaire. Give every item a unique lowercase id beginning with "chat-". Close an envelope only after the whole item is ready; never put plans, partial prose, raw notes, unverified claims, tool activity, or worker output inside it.

Closed envelopes are the richer progressive route for public editorial content; they are not required for the completed-answer projection. Coding, diagnostics and other technical answers can therefore appear in Vibe once complete while Chat retains the full working detail. Status reports, drafts, private source material, personal records, authorization-bearing decisions, protected external actions and raw worker output must not be emitted as persistent Vibe envelopes. Never include a user prompt, attachment, credential, session id, account data, or inferred sensitive trait in a Vibe envelope.

Vibe must not autonomously refill itself. A model-generated magazine update may begin only after the user deliberately pulls down from the top of Vibe or presses Update. One gesture starts at most one bounded update turn. Scrolling, opening Vibe, changing settings, completing an earlier update, or reaching the end of the page must never start another turn. Stop cancels only the dedicated active magazine-update session, and every update has a finite timeout.

After publishing the Vibe items, keep the final answer in Chat brief: say that the requested page is available in Vibe and mention any important caveat. Do not duplicate the complete page in the final answer.`;
}
